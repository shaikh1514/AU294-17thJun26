---
  - name: testing block
    hosts: serverb
    tasks:
      - name: block tasks
        block:
        - name: block task-1
          ansible.builtin.command:
            cmd: lsblk

        - name: block task-2
          ansible.builtin.command:
            cmd: ls
        when: inventory_hostname == 'serverc'
      
      - name: regular task
        ansible.builtin.debug:
          msg: this is regular task
