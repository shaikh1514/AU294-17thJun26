---
  - name: testing error modules
    hosts: serverc
    tasks:
      - name: task-1
        ansible.builtin.command:
          cmd: hostname

      - name: task-2
        ansible.builtin.fail:
          msg: This task failed intentioanlly

      - name: task-3
        ansible.builtin.command:
          cmd: date
