---
  - name: generaling myhosts file
    hosts: all
    tasks:
      - name: generate hosts file
        ansible.builtin.template:
          src: hosts.j2
          dest: /etc/myhosts
        when: inventory_hostname is in groups['dev']
