[defaults]
#this section contains default setting to be used
inventory = myinventory
remote_user = devops
ask_pass = false

[privilege_escalation]
become=true
become_user=root
become_method=sudo
become_ask_pass=false
